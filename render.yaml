services:
  - type: web
    name: salesops-backend
    env: python
    buildCommand: "pip install -r backend/requirements.txt"
    startCommand: "gunicorn salesops.wsgi:application"
    envVars:
      - key: PYTHON_VERSION
        value: 3.10